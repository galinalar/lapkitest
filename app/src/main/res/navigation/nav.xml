<?xml version="1.0" encoding="utf-8"?>
<navigation xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto" android:id="@+id/nav"
    app:startDestination="@id/shelterListActivity">

    <navigation android:id="@+id/navigation"
        app:startDestination="@id/shelterListActivity">
        <activity
            android:id="@+id/shelterListActivity"
            android:name="ru.kotlin.lapki.ShelterListActivity"
            android:label="ShelterListActivity">

        </activity>
    </navigation>

</navigation>